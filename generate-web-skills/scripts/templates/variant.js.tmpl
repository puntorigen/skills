// UI-replay variant for this generated web-action skill.
//
// Exports one async function. The runner (run_variant.js) supplies `page` and a
// `params` object (from the PW_PARAMS env var). Edit the recorded steps below
// to substitute your inputs, then run:  bash scripts/replay_ui.sh
//
// Recorded action: %%ACTION_LABEL%%  on  %%HOST%%
module.exports = async (page, params = {}) => {
%%UI_STEPS%%
};
